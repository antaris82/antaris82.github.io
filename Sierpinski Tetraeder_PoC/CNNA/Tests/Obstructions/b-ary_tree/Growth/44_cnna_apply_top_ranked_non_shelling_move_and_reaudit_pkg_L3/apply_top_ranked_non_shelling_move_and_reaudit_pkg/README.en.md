# Apply top-ranked non-shelling move and reaudit

Run:

```bash
python3 test_apply_top_ranked_non_shelling_move_and_reaudit.py --max-level 3 --growth-rules real_growth --max-boundary-faces 20 --max-pair-candidates 100 --max-rows 600 --out out
```

The packaged run includes real_growth, symmetrized_birth, and no_backreaction, executed separately with the same limits.
