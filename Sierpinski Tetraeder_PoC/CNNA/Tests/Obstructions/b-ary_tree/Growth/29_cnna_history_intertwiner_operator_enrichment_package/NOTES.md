# Notes

No `i`, no `J`, and no `J^2=-I` are introduced.

The tested enrichment tiers are derived from available growth/provenance data:

- aging and handoff injections from `D_live - D_live0` and `D_live - D_record`;
- completion/live transports from the DtN record/live pair;
- suffix-gluing identity maps from the double-history quotient rule;
- directed birth-order successor maps from sequential growth order.

This remains numerical diagnostics, not a theorem.
