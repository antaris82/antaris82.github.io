# Directed antisymmetric birth transport + pairing transport coherence gate

Run:

```bash
python3 test_pairing_transport_antisym_birth_coherence_gate.py
```

This package deliberately does **not** apply a final `sym(M)` to the tested vertex operator.  The strict control is the growth/control variant, not an operator symmetrization.
