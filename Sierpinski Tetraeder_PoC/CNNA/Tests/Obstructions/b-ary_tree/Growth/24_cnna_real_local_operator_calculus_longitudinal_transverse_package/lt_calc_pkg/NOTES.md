# Notes on relation to earlier tests

This diagnostic was built by combining the already tested mechanisms:

- Growth tests 1/2: sequential ternary births, parent-line plus older siblings as birth environment, immediate backreaction to ancestor line and older siblings, directed local response bookkeeping.
- Dynamic refresh tests 18-20: Record/Live split and the obstruction to a single operator serving both immutable provenance and live aging.
- Dynamic DtN test 22: reciprocal Schur/DtN provenance operators from current conductance, ancestor environment, descendant load, and directed response replay.
- Double-history test 23: suffix quotient / double-history gluing as a transverse handoff-defect diagnostic.

The new point is the explicit real operator calculus gate:

```text
L_p : E_p -> E_p
T_pq : E_p -> E_q
C_pq = L_q T_pq - T_pq L_p
```

This is intentionally prior to any `J` extraction.
