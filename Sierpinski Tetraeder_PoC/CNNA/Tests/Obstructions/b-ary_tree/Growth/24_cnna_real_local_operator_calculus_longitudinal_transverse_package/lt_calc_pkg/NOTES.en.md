# Notes on the relationship to earlier tests

This diagnostic was developed by combining the mechanisms that had already been tested:

- Growth tests 1/2: sequential ternary births, parent line plus older siblings as the birth environment, immediate feedback to the ancestor line and older siblings, directed local response tracking.
- Dynamic refresh tests 18–20: Record/Live split and the obstacle to a single operator serving both immutable provenance and live aging.
- Dynamic DtN test 22: reciprocal Schur/DtN provenance operators derived from current conductance, ancestor environment, descendant load, and directed response replay.
- Double-history test 23: suffix quotient / double-history gluing as a transverse handoff-defect diagnostic.

The new point is the explicit real operator calculus gate:

```text
L_p : E_p -> E_p
T_pq : E_p -> E_q
C_pq = L_q T_pq - T_pq L_p
```

This is intentionally done before any `J` extraction.
