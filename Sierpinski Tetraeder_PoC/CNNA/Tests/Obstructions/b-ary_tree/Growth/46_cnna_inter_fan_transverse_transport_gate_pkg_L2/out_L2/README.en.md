# Inter-fan transverse transport gate

Run:

```bash
python test_inter_fan_transverse_transport_gate.py --max-level 2 --make-zip
```

Outputs include move candidates for each variant, results of applied moves, JSON summaries, RESULTS.md, and SUMMARY.md.