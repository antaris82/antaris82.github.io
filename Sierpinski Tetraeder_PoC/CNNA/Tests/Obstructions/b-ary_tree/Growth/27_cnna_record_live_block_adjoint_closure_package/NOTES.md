# Notes

## Current gate

This is a `*`-precondition diagnostic, not a `J` diagnostic.

The primary question is:

```text
Does the Record/Live typed block space make the DtN-energy adjoint visible in the
growth-generated reverse operator system?
```

## Interpretation of the L5 run

The block metric is well-conditioned. The adjoint of the block handoff and of the
mixed composition defect lies inside a broad typed block rank-one envelope, but
not inside the small actually growth-generated reverse all-port family.

Thus Record/Live block typing removes the earlier metric degeneration, but it
still does not produce a closed real `*`-operator system.

## Consequence

The next object should not yet be `J`. The next diagnostic should enlarge the
generated operator system itself, e.g. by closing under finite products and sums
of longitudinal and transverse generators before applying the block adjoint.
