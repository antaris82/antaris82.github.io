# Interfan phase propagation obstruction gate

Run:

```bash
python3 test_interfan_phase_propagation_obstruction_gate.py
```

This package extends the directed antisymmetric birth-transport + pairing-transport test.  It deliberately does **not** apply a final `sym(M)` to the tested vertex operator.
