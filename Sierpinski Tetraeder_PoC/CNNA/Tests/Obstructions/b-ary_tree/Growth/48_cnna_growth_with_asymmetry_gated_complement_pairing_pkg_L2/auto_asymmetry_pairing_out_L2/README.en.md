# Automatic asymmetry-gated complement pairing package

Run:

```bash
python3 test_growth_with_asymmetry_gated_complement_pairing.py --max-level 2 --max-auto-pairings 1 --out auto_pairing_out_L2
```

The default run is intentionally small and auditable. Increase `--max-level` and `--max-auto-pairings` only after reviewing the logs.
